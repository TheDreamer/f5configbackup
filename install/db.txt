PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE DEVICES (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
NAME TEXT NOT NULL,
IP CHAR(15),
CID_TIME INT,
DATE_ADDED INT,
LAST_DATE INT
);
CREATE TABLE JOBS (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
DATE DATE NOT NULL,
TIME INT NOT NULL,
ERRORS INT,
COMPLETE BOOLEAN,
DEVICE_TOTAL INT,
DEVICE_COMPLETE INT,
DEVICE_W_ERRORS TEXT
);
CREATE TABLE MOTD (
ID INT,
MOTD TEXT);
INSERT INTO "MOTD" VALUES(1,'WARNING: Unauthorized access to this system is forbidden and will be prosecuted by law. By accessing this system, you agree that your actions may be monitored if unauthorized usage is suspected. ');
CREATE TABLE ADMIN (
ID INT,
HASH VARCHAR(50));
INSERT INTO "ADMIN" VALUES(1,'$2a$12$g5hlenlIbNTX4YPE/ICKd.pTOwuJTmzlWF7EA90/oZ3cYauxf9gmG');
CREATE TABLE USERS (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
NAME VARCHAR(30),
HASH VARCHAR(100),
ADDED_BY VARCHAR(30),
DATE_ADDED INT
);
CREATE TABLE SETTINGS_INT (
NAME VARCHAR(30),
VALUE INT);
INSERT INTO "SETTINGS_INT" VALUES('timeout',1200);
CREATE TABLE ARCHIVE (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
DEVICE INT,
DIR VARCHAR(50),
FILE VARCHAR(50)
);
COMMIT;
