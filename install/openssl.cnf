####################################################################
# F5backup openssl config
####################################################################
[ req ]
default_bits            = 2048
default_md              = sha1
prompt = no
encrypt_key = no
default_keyfile         = privkey.pem
distinguished_name = dn

[ dn ]
O = f5backup
CN = f5backup.localhost
